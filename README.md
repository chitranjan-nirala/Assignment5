# **Module 6: Data Structures and Strings in Python**

## **Overview**  
This module focuses on working with Python's core data structures — dictionaries and lists — along with string manipulation techniques like slicing and reversing.

---

## **Task 1: Create a Dictionary of Student Marks**

### **Problem Statement**  
Write a Python program that:  
1. Creates a dictionary where student names are keys and their marks are values.  
2. Asks the user to input a student's name.  
3. Retrieves and displays the corresponding marks.  
4. If the student’s name is not found, display an appropriate message.  

### **Expected Output**  

#### ✅ If the student exists:

#### ❌ If the student does not exist:

### **Implementation Details**  
- Use a predefined dictionary (e.g., `{'Alice': 85, 'Bob': 90}`)  
- Use `.get()` method or `if-else` to check if the student name exists  
- Handle user input with appropriate message formatting  

---

## **Task 2: Demonstrate List Slicing**

### **Problem Statement**  
Write a Python program that:  
1. Creates a list of numbers from 1 to 10.  
2. Extracts the first five elements from the list.  
3. Reverses these extracted elements.  
4. Prints both the extracted list and the reversed list.  

### **Expected Output**

### **Implementation Details**  
- Use `list(range(1, 11))` to generate numbers  
- Use slicing `[:5]` to extract elements  
- Use `[::-1]` to reverse a list  

---

## **How to Run the Programs**  
1. Save the Python scripts (`task1.py` and `task2.py`) in the same directory as this README file.  
2. Run the scripts using Python:  
   ```bash
   python task1.py
   python task2.py
### **Author**  
[Your Name]  
